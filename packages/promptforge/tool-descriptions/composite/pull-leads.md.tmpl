---
name: leadbay_pull_leads
kind: tool-description
short_description: |
  Pull new leads — today's prospects, last-active lens. Render as a
  3-col markdown table sorted by score desc; col 1 is the inline-code
  10-segment `▰❖▱` bar + linked company + location + size; col 2 is
  why-fits ≤20 words; col 3 is linked contact + title.
routing:
  triggers:
    - "show me leads"
    - "today's prospects"
    - "best new leads"
  anti_triggers:
    - phrase: "leads I should follow up with"
      route_to: leadbay_pull_followups
    - phrase: "I'm going to <city>"
      route_to: leadbay_followups_map
    - phrase: "reach out to"
      route_to: leadbay_pull_followups
    - phrase: "get back to"
      route_to: leadbay_pull_followups
    - phrase: "contact today"
      route_to: leadbay_pull_followups
    - phrase: "should I contact"
      route_to: leadbay_pull_followups
    - phrase: "reconnect with"
      route_to: leadbay_pull_followups
    - phrase: "re-engage"
      route_to: leadbay_pull_followups
  prefer_when: "fresh Discover leads; if a lens is named, pass `lensId` and pin it"
  examples:
    positive:
      - "Show me today's leads."
      - "Pull my best new prospects."
    negative:
      - "Which leads should I follow up with this week?"
      - "I'm flying to Berlin Thursday — who should I meet?"
      - "Show me leads I should reach out to today."
      - "Who should I get back to today?"
      - "Leads I should contact today."
rendering_hint: |
  3-col markdown table sorted by `score` desc — DO NOT print the numeric
  score. Col 1 = inline-code 10-segment bar (`▰` firmographic, `❖` AI
  booster cap at the right end of the filled run, `▱` empty;
  filled=round(score/10), ai=round(avg_boost/3.3)) + `<br>` + linked
  company · location · size. Col 2 = why-fits ≤20 words. Col 3 = linked
  contact + title. Full algorithm + linking rules below.
next_steps: pull-leads
annotations:
  readOnlyHint: true
  destructiveHint: false
  idempotentHint: true
  openWorldHint: true
---
Pull up new leads from the user's last-active lens — the canonical "show me today's prospects" tool.

Leadbay works like an inbox: each time the user logs back in, a fresh batch is delivered, paced by how many leads they've actually acted on recently. Pulling more won't produce more; user outreach/skips/saves does. Each returned lead carries a one-line `qualification_summary` built from leadbay_ai_agent_responses, plus the rich tags / scores / engagement counters / in-flight flags from the lead summary.

Roughly the top 10 of the batch come pre-qualified (populated qualification_summary + ai_agent_lead_score); leads below the top ~10 carry only the basic firmographic `score` — not worse, just resource-saved by the system. Call leadbay_bulk_qualify_leads to deepen any of them on demand — a healthy daily rhythm is to bulk-qualify the rows without ❖ caps so tomorrow's top-10 list is richer.

Every lead carries `recommended_contact` (with `linkedin_page` when the backend has it), `phone_numbers` (when available), `split_ai_summary.{worth_pursuing, approach_angle, next_step}` (when AI-qualified), and `social_urls` per-platform (linkedin, instagram, tiktok, facebook, twitter, crunchbase). Use them — they're already in the response, you don't need a second call.

{{include:headers/tool-when-to-use}} as the agent's default opening move when the user wants to see leads, or as a daily check-in for what's new today.

{{include:headers/tool-when-not-to-use}} when the user has named a specific lens — pass `lensId` to override the auto-resolution. Replaces the older leadbay_find_prospects (removed in v0.2.0).

The active lens can change between calls (5-min cache + backend `last_requested_lens`). If a multi-step workflow depends on staying on one lens, **capture `response.lens.id` from the first response and pass it as the `lensId` argument on every subsequent Leadbay call** — including re-pulls, bulk qualifies, and research. (Field-name caveat: response nests it as `lens.id`; the parameter is `lensId`.) Re-pulling without `lensId` after a long-running tool may silently switch to a different lens and discard prior work.

---

{{include:rendering/pull-leads-table}}

---

{{include:next-steps/pull-leads}}
