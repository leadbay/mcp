---
name: leadbay_add_note
kind: tool-description
short_description: |
  Add a free-form note to a lead. Notes are visible to the whole
  organization in Leadbay.
annotations:
  readOnlyHint: false
  destructiveHint: true
  idempotentHint: false
  openWorldHint: true
---
Add a note to a lead. Notes are visible to the whole organization in Leadbay.

{{include:headers/tool-when-to-use}} low-level — for free-form notes not tied to outreach actions, including meaningful per-lead notes/context preserved from an imported file after the import returns lead IDs.

{{include:headers/tool-when-not-to-use}} to log an outreach action — use leadbay_report_outreach, which requires verification (gmail/calendar/user_confirmed) to prevent hallucinated outreach poisoning the SDR pipeline.

{{include:headers/verification-required-if-write}}
