---
name: leadbay_clear_selection
kind: tool-description
short_description: |
  Clear the user's transient selection.
annotations:
  readOnlyHint: false
  destructiveHint: true
  idempotentHint: true
  openWorldHint: true
---
Clear the user's transient selection.

{{include:headers/tool-when-to-use}} cleanup after manual selection work, or recovery from a stuck composite.

{{include:headers/tool-when-not-to-use}} in normal flow — composites clear in their own finally blocks.

{{include:headers/verification-required-if-write}}
