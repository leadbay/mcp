{
  "name": "@leadbay/core",
  "version": "0.8.10",
  "private": true,
  "description": "Leadbay shared core: HTTP client and protocol-agnostic tool definitions. Consumed by @leadbay/mcp.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm --filter @leadbay/components build && pnpm --filter @leadbay/promptforge build",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:smoke": "vitest run --config vitest.smoke.config.ts"
  },
  "dependencies": {
    "fastest-levenshtein": "^1.0.16",
    "zod": "^3.23.8"
  },
  "license": "MIT"
}
